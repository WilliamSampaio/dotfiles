#!/usr/bin/env bash
# Distro Ubuntu 22.0

sudo cp -r "$HOME/.config/nvim" dotfiles/.config/
sudo cp -r "$HOME/.vim" dotfiles
sudo cp "$HOME/.gitconfig" dotfiles
sudo cp "$HOME/.vimrc" dotfiles
sudo cp "$HOME/.zshrc" dotfiles